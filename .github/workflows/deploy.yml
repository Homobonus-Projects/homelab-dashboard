name: Deploy Dashboard

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Nginx
        run: |
          # Kopiujemy plik
          cp index.html /var/www/dashboard/index.html
